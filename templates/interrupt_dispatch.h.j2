
#include <metal/drivers/{{ driver_string }}.h>

/* These defines "redirect" the calls to the public Freedom Metal interrupt API
 * to the driver for the controller at compile time. Since they are the same
 * as the actual public API symbols, when they aren't defined (for instance,
 * if the Devicetree doesn't properly describe the interrupt parent for the device)
 * they will link to the stub functions in src/interrupt.c
 */

#define metal_interrupt_init(intc) {{ driver_string }}_init((intc))

#define metal_interrupt_set_vector_mode(intc, mode) {{ driver_string }}_set_vector_mode((intc), (mode))
#define metal_interrupt_get_vector_mode(intc) {{ driver_string }}_get_vector_mode((intc))

#define metal_interrupt_set_privilege(intc, privilege) {{ driver_string }}_set_privilege((intc), (privilege))
#define metal_interrupt_get_privilege(intc) {{ driver_string }}_get_privilege((intc))

#define metal_interrupt_clear(intc) {{ driver_string }}_clear((intc))
#define metal_interrupt_set(intc) {{ driver_string }}_set((intc))

#define metal_interrupt_register_handler(intc, id, handler, priv_data) {{ driver_string }}_register_handler((intc), (id), (handler), (priv_data))
#define metal_interrupt_register_vector_handler(intc, id, handler, priv_data) {{ driver_string }}_register_vector_handler((intc), (id), (handler), (priv_data))

#define metal_interrupt_enable(intc, id) {{ driver_string }}_enable((intc), (id))
#define metal_interrupt_disable(intc, id) {{ driver_string }}_disable((intc), (id))

#define metal_interrupt_set_threshold(intc, level) {{ driver_string }}_set_threshold((intc), (level))
#define metal_interrupt_get_threshold(intc) {{ driver_string }}_get_threshold((intc))

#define metal_interrupt_set_priority(intc, id, prio) {{ driver_string }}_set_priority((intc), (id), (prio))
#define metal_interrupt_get_priority(intc, id) {{ driver_string }}_get_priority((intc), (id))

#define metal_interrupt_set_preemptive_level(intc, id, level) {{ driver_string }}_set_preemptive_level((intc), (id), (level))
#define metal_interrupt_get_preemptive_level(intc, id) {{ driver_string }}_get_preemptive_level((intc), (id))

#define metal_interrupt_vector_enable(intc, id) {{ driver_string }}_vector_enable((intc), (id))
#define metal_interrupt_vector_disable(intc, id) {{ driver_string }}_vector_disable((intc), (id))

#define metal_interrupt_affinity_enable(intc, bitmask, id) {{ driver_string }}_affinity_enable((intc), (bitmask), (id))
#define metal_interrupt_affinity_disable(intc, bitmask, id) {{ driver_string }}_affinity_disable((intc), (bitmask), (id))

#define metal_interrupt_affinity_set_threshold(intc, bitmask, level) {{ driver_string }}_affinity_set_threshold((intc), (bitmask), (level))
#define metal_interrupt_affinity_get_threshold(intc, context_id) {{ driver_string }}_affinity_get_threshold((intc), (context_id))

